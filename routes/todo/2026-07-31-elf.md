---
date: 2026-07-01
title: (Linking, Part I) Writing a ELF linker
layout: writing
---

This is the first part in a four part[^1] series on writing barebones, toy linkers for educational purposes. This part details the ELF format by writing a x86-64 barebones static linker; the next part will take a look at extending the linker to support PIEs (position-independent executables) and maybe will detail arm64 differences; the third part will detail dynamic linking; the final part will detail the Mach-O format used on Apple OSes[^2]. I highly recommend you take a look at [Zig in Y Minutes]() for pregaming as the code will be in Zig v0.16.0. Also, use [`zvm`]() to install the requisite version for convenience. Notably, we'll use

[^1]: Maybe there may be some extra parts, I haven't really fleshed this out fully.

[^2]: Sorry if you're a Windows person... I don't feel like looking at [PE]().

Since this code details x86-64 ELF you will need to run the linked binary in an environment that supports x86-64. (I'll shill my own product, [Rukh]() here, which lets you spin up microVMs; you can run `rukh . --arch x86-64` in the directory you're working in and instantly get a working environment.)

tl;dr of things we will be doing:

- Figuring out how to link a set of relocatable objects into our very first runnable executable
- Get static libraries working with our linker

Things we will not be doing:

- Focusing on optimization of any form or sort.

## What is a linker

A linker is just a program that combines various pieces of code and data into one file that can then be loaded and executed. Different OSes support different formats. `ld` on Linux is used to invoke the linker, for example, and it's just a binary.

I think it's helpful to take a look at what happens exactly when you run a compiler driver. Usually, it follows a set of steps:

1. Some sort of **preprocessor** runs on your code. As an analogy, I like to think of this as a find + replace operation with constants (e.g., files, macros, etc.) if you will. In C, this is what header files are
2. Some sort of **compiler** runs on your preprocessed code, compiling down into an intermediate representation (IR). Compilers also tend to do extra things to facilitate a easier time for the next couple of steps, e.g. optimizations, mangling the names of functions, etc etc.
3. Some sort of **assembler** runs on your compiled code, compiling that IR down to your instruction set architecture (ISA) of choice. This typically depends on your host system - i.e., typically represented
4. Some sort of **linker** runs on your assembled code, combining it with other assembled code to form an execuable. The file a linker produces, the **executable**, can usually be executed in the right conditions (i.e. supported architecture, etc.)

You can semantically combine the first three and refer to them as a **translator**. In addition, these steps are typically run by a **compiler driver**, e.g. GNU or Clang, which are programs that simply abstract away all these steps with the options you pass in.

Linkers are a useful, necessary step in modern systems because besides producing working executables, it's an extra optimization step[^3]. For example, many modern systems have the concept of shared libraries - i.e., shared code. For example, using when you use `printf`

[^3]: Many such cases.

![todo Here goes a diagram of this stuff]()

I think a good way to understand this a little bit more and to motivate writing a linker is to look at this process through the lens of trying to build and then run an actual program. Let's take a look at a "Hello, world!" program of sorts.

### First, an example program

Here's an example `main.c`:

```c
#include <stdio.h>

int sum(int *a, int n);

static int count = 42;
static int array[3] = {1, 2, 5};

int main() {
  printf("Hello, world!\n");
  count += sum(array, 3);
  return count;
}
```

As you can see, we define a function called `sum` that hasn't actually been defined yet. This is because we've defined it in `sum.c`:

```c
static int count = 4;

int sum(int *a, int n) {
  int i, s = 0;

  for (i = 0; i < n; i++) {
    s += a[i];
  }

  return s + count;
}
```

We would expect the output to look something like this:

```
Hello, world!
echo $?
54
```

If we were to work through each of the steps of the build process, manually, it might look something like this.

First off, we can run just the preprocessor

We start out with the preprocessor. In _main.c_, we import _stdio.h_.

1. Preprocessor: In _main.c_, we import _stdio.h_. This is par
2. Compiler:
3. Assembler:

Now let's talk about the linker!

### Looking at an ELF

Let's see what's inside an ELF file. To do this, we're going to write a parser. This is

And here's an interactive version of this,

(This is a hex viewer generated by an LLM, making use of [_elf64.zig_]() compiled to WebAssembly. _elf64.zig_ is the handwritten ELF64 parser we'll writing.)

A section group is a mechanism for grouping related sections so that the linker can treat them as a unit, represented by a section with `sh_kind = .SHT_GROUP`. It groups together sections so that the linker can treat them as a unit. This is useful for TODO

This is followed by a

If you're interested in compiling to WebAssembly

I had an LLM generate

### At runtime

So at this point in time we have a executable. What happens when we run `./main`?

We inject

The kernel is responsible for then loading the, i.e. there is a kernel-space program called

### Summing up

Hopefully this example gives you an idea of what a linker does in terms of the bare minimum.

## Linkers: a bit more info

## Let's write the linker

### Shared libraries

Static shared libraries are [archive files](<https://en.wikipedia.org/wiki/Ar_(Unix)>)

### On memory

I'm going to take a quick detour to discuss memory in modern systems. If you know about this stuff you can just skip this

Early CPUs started out with **segmentation**. The 8086[^8086], which was a 16-bit microprocessor, introduced segmentation to increase the amount of addressable memory from 16 bits to 20 bits - i.e., from 64KiB to 1MiB.

[^8086]: Random, but I like to remember

Segmentation introduces the notion of _segments_; a segment is a virtual view into physical memory; how far is this virtual address from the actual address, i.e., an offset? They're just values stored in a segment register: `CS` for code segment; `SS` for stack segment;

In the initial version of segmentation, segment registers directly contained the offset. Protected mode was introduced later on (in x86, enabled by toggling bit in control register `CR0`) that introduces the notion of a _local/global descriptor table_ (LDT/GDT), with the segment register containing an index into one of these tables. The tables themselves contains a _descriptor_ for the segment - the size, permissions, etc. The descriptor

This last part might resonate with you

Segmentation introduced the notion of **virtual memory**: applying transformations on a virtual address to a physical address. That way, we can have multiple processes running on theoretically fixed addresses - which is

![todo segmentation model]()

The problem with segmentation is that it introduces **fragmentation**: we need to be constantly moving things around in memory for things to fit. As a result, segmentation isn't really utilized nowadays, except for backward compatibility reasons. (For example: booting conventions still tend to make use of segmentation to make the jump from 16-bit compatibility mode, termed real mode, to protected mode, and then finally to long mode.)

Nowadays **paging** is used. Paging keeps the notion of virtual addresses, but introduces the concept of _pages_ rather than segments. Pages are just fixed blocks of memory; 4KiB is the typical page/frame size on x86-64. Then, instead of having the local/global descriptor table, we have

The nice thing about this is that virtual addresses on x86-64 directly map to page table entries (PTE). Each table index is 9 bits ($2^9 = 512$ entries), and the final offset is 12 bits. The remaining two bytes ($64 - (9 * 4 = 36 + 12 = 48) = 16$) are currently empty.

![todo paging model]()

Trying to access memory outside

To improve performance, the

The other upside of paging is that the kernel can swap pages out to disk when they are infrequently/no longer accessed.

[_Introduction To Paging_]() is a probably better-worded version of this. I just thought this would be useful for gaining a intuition for why we are forced to do specific things:

- Why do we

## Testing the linker

Writing tests would have been good... I had an LLM write the test cases after I described them. You can find them in [_test.zig_]().

I also had an LLM write a version of `readelf` to test linker output

## For fun

Linux has a

## Et cetera

To write this I had to revisit code I wrote. The first time I wrote this code it was in Zig 0.15. Zig 0.16 introduces breaking changes, notably the [new I/O pattern]() where I/O becomes a more defined interface that can be passed around similar to the [`Allocator`]() interface. This allows the implementor to do stuff like figure out concurrency primitives etc., similar to how Rust asks for a concurrent runtime if you're writing concurrent code. It took me approximately an hour[^99] to get the old code working before I could make changes.

[^99]: Not including my ADHD distraction tendencies.

I think this makes Zig somewhat inconvenient; it seems it's been in 0.x.x for almost a decade now? Zig is wonderful and I've been fooling around with it since 2023 (it's a lot more fun to write Zig over C, this makes sense because in my eyes Zig is a direct descendant of C), but it can be a bit of a headache. I almost gave up and was seriously considering using Rust or something but the comptime reader/writer pattern is just perfect for education purposes (vs. having a Rust macro or using a crate or doing it manually in Rust). It does seem like the creator has mentioned getting to a 1.0 release soon so fingers crossed.

The first time I wrote this code, I was very aware of a very bad code smell: I was mutating.

The source code is [here](), keep in mind that some things might have changed since this post but it'll be relatively the same.

For references, please see [this]() list of bookmarks. Notably, in this post I haven't mentioned

Also, chapter seven of _Computer Systems:_ was useful. _Build a Debugger_ (which I briefly skimmed through) also might be useful.
