-- The general gist - run a command map for every file that isn't .html.
-- For .html files, in `components` are ignored. 
--
-- Then, we loop over the 

main :: IO ()
main = do
  putStrLn "Hello, world!"
